export interface ClassOption {
  id: string;
  label: string;
  category: string;
}

export interface SubjectOption {
  id: string;
  label: string;
}

export interface ChapterOption {
  id: string;
  label: string;
}
